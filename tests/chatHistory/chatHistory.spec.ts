// import { test, expect } from '../../fixtures/auth';

// test('Chat history loads', async ({ pageAuthenticated }) => {
//   await expect(pageAuthenticated.getByText('Chat History')).toBeVisible();
// });
