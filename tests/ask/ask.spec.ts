// import { test, expect } from '../../fixtures/auth';
// import { goToAsk } from '../../helpers/navigation';
// import { chatPage } from '../../helpers/selectors';

// test('Ask feature test', async ({ pageAuthenticated }) => {
//   const page = pageAuthenticated;

//   await goToAsk(page);
//   await page.fill(chatPage.askBox, 'What is AI?');
//   await page.click(chatPage.sendButton);

//   await expect(page.locator(chatPage.messageBubble)).toContainText('AI');
// });
