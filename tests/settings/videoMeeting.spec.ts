// import { test, expect } from '../../fixtures/auth';
// import { goToSettings } from '../../helpers/navigation';
// import { settingsPage } from '../../helpers/selectors';

// test('Video meeting settings loads', async ({ pageAuthenticated }) => {
//   await goToSettings(pageAuthenticated);
//   await pageAuthenticated.click(settingsPage.videoMeeting);
//   await expect(pageAuthenticated.getByText('Video meeting')).toBeVisible();
// });
