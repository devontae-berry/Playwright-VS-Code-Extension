// import { test, expect } from '../../fixtures/auth';
// import { Page } from '@playwright/test';
// import { goToForms } from '../../helpers/navigation';

// test('Forms feature loads', async ({ pageAuthenticated }) => {
//   const page = pageAuthenticated;

//   await goToForms(page);
//   await expect(page.getByText('Forms')).toBeVisible();
// });
