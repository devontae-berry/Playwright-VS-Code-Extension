// import { test, expect } from '../../fixtures/auth';

// test('Feedback feature loads', async ({ pageAuthenticated }) => {
//   await pageAuthenticated.goto('/feedback');
//   await expect(pageAuthenticated.getByText('Feedback')).toBeVisible();
// });


// // import { test, expect } from '../../fixtures/auth';

// // test.describe('Feedback', () => {
// //   test('submit feedback', async ({ pageAuthenticated }) => {
// //     const page = pageAuthenticated;
// //     await page.goto('/feedback');

// //     const feedbackInput = page.getByPlaceholder('How was your experience?');
// //     await feedbackInput.fill('Automated feedback: everything looks good!');
// //     await page.getByRole('button', { name: 'Send feedback' }).click();

// //     await expect(page.getByText('Thanks for your feedback')).toBeVisible();
// //   });
// // });
