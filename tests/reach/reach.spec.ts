// import { test, expect } from '../../fixtures/auth';
// import { goToReach } from '../../helpers/navigation';

// test('Reach feature loads', async ({ pageAuthenticated }) => {
//   const page = pageAuthenticated;

//   await goToReach(page);
//   await expect(page.getByText('Reach')).toBeVisible();
// });
