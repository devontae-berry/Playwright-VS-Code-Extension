// import { test, expect } from '../../fixtures/auth';

// test('Profile update loads', async ({ pageAuthenticated }) => {
//   const page = pageAuthenticated;

//   await page.goto('/profile');
//   await expect(page.getByText('Profile')).toBeVisible();
// });
