// import { test, expect } from '../../fixtures/auth';
// import { goToLinks } from '../../helpers/navigation';

// test('Links feature', async ({ pageAuthenticated }) => {
//   const page = pageAuthenticated;

//   await goToLinks(page);
//   await expect(page.getByText('Links')).toBeVisible();
// });
